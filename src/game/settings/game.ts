import { IGameSettings } from '@byte-eight-engine/engines';

const gameSettings: IGameSettings = {
  canvasSize: {
    columns: 20,
    rows: 20,
  },
  scale: 2,
  blockSize: 16,
};

export { gameSettings };
